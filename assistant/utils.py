# assistant/utils.py

# You can add utility functions here, like error handling, string formatting, etc.
def log_error(message):
    print(f"ERROR: {message}")

def format_text(text):
    return text.strip().lower()
